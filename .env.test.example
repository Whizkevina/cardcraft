# E2E test environment — copy to `.env.test` and fill in values.
# Do not commit `.env.test` (it is gitignored).

DATABASE_URL=postgresql://user:password@localhost:5432/cardcraft_test
SESSION_SECRET=test-session-secret-min-32-chars-long
NODE_ENV=development
PORT=5000
HOST=127.0.0.1
E2E_BASE_URL=http://127.0.0.1:5000
# Set to true only when DATABASE_URL points at a dedicated test database (truncates users/templates/projects).
# E2E_RESET_DB=true

PAYSTACK_SECRET_KEY=sk_test_placeholder
PAYSTACK_PUBLIC_KEY=pk_test_placeholder
APP_URL=http://127.0.0.1:5000
